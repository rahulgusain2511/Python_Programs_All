random() : this function genrates random numbers from the range
            [0,1)
            for example
            <module>.<functionName>()
            random.random() :  0.85267173563351

randint(a,b) : this function return random integer in the range of
                [a, b]

            random.randint(1,5) : 1,2,3,4,5 (randomly)

randrange(a, b, step) : this function return random integer in the
                range of [a, b) where step can be used as a jump.

                random.randrange(1,10,2) : 1,3,5,7,9 (randomly)
                random.randrange(0,10,2) : 0,2,4,6,8 (randomly)

uniform(a, b) : return floating number    [a,b)

choice(Sequence): random selection from a sequence.
              random.choice([2,3,4,5,6])

shuffle(list) : This method is used to shuffle or swap the contents of
                a list.

                random.shuffle(list)
